<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mapper.UserMapper">
  <resultMap id="BaseResultMap" type="com.egouer.admin.auth.domain.User">
    <!--
      WARNING - @mbggenerated
      This element is automatically generated by MyBatis Generator, do not modify.
      This element was generated on Sun Apr 17 01:16:28 CST 2016.
    -->
    <id column="userId" jdbcType="BIGINT" property="userid" />
    <result column="userName" jdbcType="VARCHAR" property="username" />
    <result column="password" jdbcType="VARCHAR" property="password" />
    <result column="status" jdbcType="VARCHAR" property="status" />
    <result column="addTime" jdbcType="TIMESTAMP" property="addtime" />
    <result column="updateTime" jdbcType="TIMESTAMP" property="updatetime" />
  </resultMap>
  
  <resultMap id="BaseResultMap1" type="com.egouer.admin.auth.domain.User">
    <id column="userId" jdbcType="BIGINT" property="userid" />
    <result column="userName" jdbcType="VARCHAR" property="username" />
    <result column="status" jdbcType="VARCHAR" property="status" />
    <result column="addTime" jdbcType="TIMESTAMP" property="addtime" />
  </resultMap>
  
  <sql id="Base_Column_List">
    <!--
      WARNING - @mbggenerated
      This element is automatically generated by MyBatis Generator, do not modify.
      This element was generated on Sun Apr 17 01:16:28 CST 2016.
    -->
    userId, userName, status, addTime, updateTime
  </sql>
  
  <select id="checkUser" parameterType="com.egouer.admin.auth.domain.User" resultType="com.egouer.admin.auth.domain.User">
  	select <include refid="Base_Column_List"/> from User t 
  	where t.userName = #{username} and t.password = #{password} and t.status = #{status}
  </select>
  
  <select id="selectUserList" parameterType="com.egouer.admin.auth.domain.User" resultMap="BaseResultMap1">
  	select
		t.userId,
		t.userName,
		t.`status`,
		t.addTime
	from
		User t
	where 1=1 
	<if test="status != null">
		and t.`status` = #{status}
	</if>
	<if test="username != null">
		and t.userName like '%${username}%'
	</if>
	order by
		t.userId desc limit ${offset},${pagesize}
  </select>
  
  <select id="selectUserListCount" parameterType="com.egouer.admin.auth.domain.User" resultType="long">
  	select
		count(1)
	from
		User t
	where 1=1
		<if test="status != null">
		and t.`status` = #{status}
	</if>
	<if test="username != null">
		and t.userName like '%${username}%'
	</if>
  </select>
  
  <select id="selectUserById" parameterType="com.egouer.admin.auth.domain.User" resultType="com.egouer.admin.auth.domain.User">
  	select <include refid="Base_Column_List"/> from User t where t.userId = #{userid}
  </select>
  
  <update id="updateUserById" parameterType="com.egouer.admin.auth.domain.User">
  	update User t set t.userName = #{username}, t.updateTime = now() where t.userId = #{userid}
  </update>
  
  <insert id="addUser" parameterType="com.egouer.admin.auth.domain.User">
  	insert User(userName,password,addTime,`status`) values (#{username},#{password},now(),#{status})
  </insert>
</mapper>